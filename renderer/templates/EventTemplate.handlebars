var {{{streamName}}};
(function(){
  var widget = window.shadowRoots["{{{widgetUID}}}"];
  {{{streamName}}} = Bacon.mergeAll(
    $(widget.children).asEventStream("{{{eventType}}}", "{{{elementSelector}}}")
  );
})();