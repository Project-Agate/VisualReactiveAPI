var doc = document.currentScript.ownerDocument;
var placeholder = doc.querySelector("{{{selector}}}");
var shadow = placeholder.createShadowRoot();
var importedContent = doc.querySelector('link[href="{{{childFilePath}}}"]').import.documentElement;
shadow.appendChild(importedContent);