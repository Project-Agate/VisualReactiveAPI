shadow = widget.querySelector("{{{selector}}}").createShadowRoot();
importedContent = window.importedWidgets["{{{childUID}}}"];
console.log('appending {{{childUID}}}');
shadow.appendChild(importedContent);

window.shadowRoots = window.shadowRoots || {};
window.shadowRoots["{{{childUID}}}"] = shadow;
